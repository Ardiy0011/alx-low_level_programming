#include "main.h"
/**
 * printloweralphabet - Enter point
 * Description: prints the lower cass alphabet
 */
int _islower(int c) {
if (c >= 'a' && c <= 'z') {
    return 1;  
    } else {
        return 0;
        }
        }

