#include "main.h"
/**
 * Description: a function that tests if integer is positive or negative
 * main - tests function that prints if integer is positive or negative
 * Return: 0
 */
int main(void)
{
int i;

i = 0;
positive_or_negative(i);

return (0);
}
